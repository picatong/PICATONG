window.onload = function() {
  var button = document.getElementByID("night");
  button.addEventListener("night", displayTime. false);
  button.addEventListener("night", function(e) { console.log("second listener:" + e); }, false);
}
